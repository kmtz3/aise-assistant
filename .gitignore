# Personal / per-install files — never commit these
about/identity.md
about/voice.md
about/workspace.md

# Claude workspace — settings, plans, agents (private dev artefacts)
.claude/

# macOS
.DS_Store

# Build artifacts
*.plugin
dist/

# Runtime output dirs (generated per-user)
diagrams/
memory/
